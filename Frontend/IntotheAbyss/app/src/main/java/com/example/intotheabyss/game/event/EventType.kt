package com.example.intotheabyss.game.event

/**
 * An Enum to determine which event type the network should process.
 * @author Benjamin Vogel
 */
enum class EventType {
    ATTACK,
    JOIN,
    REQUEST,
    KICK
}